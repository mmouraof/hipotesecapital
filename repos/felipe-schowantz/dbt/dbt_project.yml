name: "hipotetical_fia"
version: "1.0.0"
config-version: 2

profile: "hipotetical_fia"

model-paths:      ["models"]
test-paths:       ["tests"]
snapshot-paths:   ["snapshots"]
analysis-paths:   ["analyses"]
macro-paths:      ["macros"]
seed-paths:       ["seeds"]
target-path:      "target"
clean-targets:    ["target", "dbt_packages"]

models:
  hipotetical_fia:
    staging:
      +materialized: view
      +schema: staging
    gold:
      +materialized: table
      +schema: gold
